backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
frontend:
  framework: react
  phases:
    build:
      commands:
        - npm install
        - npm build
    deploy:
      commands:
        - amplify publish